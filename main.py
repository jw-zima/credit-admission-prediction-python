# placeholder to run all steps required to lead the data, build and select features, preprocess training and test set, fit final model
import params
print(params.data_raw_location)
print(params.seed)
